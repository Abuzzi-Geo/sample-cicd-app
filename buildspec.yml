version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing dependencies

  build:
    commands:
      - echo Build Started
      - npm install

  post_build:
    commands:
      - echo Running tests
      - npm test

artifacts:
  files:
    - '**/*'